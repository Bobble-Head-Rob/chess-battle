# Local Notes

Expected v0 stack:
- static HTML
- CSS
- JavaScript
- local Python or Node static server only if helpful

Useful local commands:
- `python3 -m http.server 18999 --bind 127.0.0.1`
- `python3 -m py_compile` for Python files if any exist
- browser at `http://127.0.0.1:18999/`
